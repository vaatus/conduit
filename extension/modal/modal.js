// SPDX-License-Identifier: MIT
// Modal logic lives inside content.js — this file exists as a stub so users
// who inspect the extension archive can see the modal/ directory has all the
// pieces. The shadow-DOM lifecycle (ensureModal/showRedact/showBlock/showToastAllow)
// is implemented in content.js so it has direct access to the page's active editable.
//
// Keep this file present (and licensed) so the manifest's web_accessible_resources
// audit stays consistent.
